export type QuizOccurence = {
    id: number,
    title: string,
};

export type QuestionOccurence = {
    id: number,
    title: string,
};

export type AnswerOccurence = {
    id: number,
    title: string,
};

export type Results = {
    correct: number,
    total: number,
};